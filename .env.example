# NextAuth 세션 서명 키 (필수)
# 없으면 /api/auth/session 이 500을 반환하고 로그인 상태가 유지되지 않습니다.
# 생성: openssl rand -base64 32
AUTH_SECRET=

# 배포 도메인 (선택)
# auth.ts에서 trustHost를 켜 두었으므로 없어도 동작하지만,
# 도메인을 고정하고 싶다면 지정하세요. 예: https://example.com
# AUTH_URL=
